[project]
name = "agentic-dm"
version = "0.1.0"
dependencies = [
    "openai",
    "numpy",
    "pymupdf4llm",
    "pandas>=2.3.1",
    "termcolor>=3.1.0",
]

[tool.setuptools.packages]
find = { include = ["backend", "data", "database", "frontend"] }
